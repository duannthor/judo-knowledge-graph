{
  "name": "judo-knowledge-graph",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static-first knowledge graph for judo techniques, legality, safety, and combo mechanics.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "astro": "^5.8.0",
    "typescript": "^5.9.0"
  }
}
